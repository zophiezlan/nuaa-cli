# =============================================================================
# NUAA CLI Environment Configuration
# =============================================================================
# Copy this file to .env and configure your settings
# The .env file is gitignored and will not be committed to the repository

# =============================================================================
# GitHub Authentication (Optional)
# =============================================================================
# GitHub Personal Access Token for higher API rate limits
# Without a token: 60 requests/hour
# With a token: 5,000 requests/hour
#
# To create a token:
# 1. Visit: https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Select scopes: public_repo (for public repositories)
# 4. Copy the generated token
#
# You can use either GH_TOKEN or GITHUB_TOKEN (both are supported)

# GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Localization & Language (Optional)
# =============================================================================
# Set the interface language
# Supported languages: en, es, ar, vi, zh_CN, th
# Default: Auto-detected from system locale

# LANGUAGE=en

# Specific locale settings (advanced)
# LC_ALL=en_US.UTF-8
# LANG=en_US.UTF-8

# =============================================================================
# Logging Configuration (Optional)
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO

# LOG_LEVEL=INFO

# Custom log file location
# Default: ./nuaa-cli.log (in current directory)
# Note: Future versions will use platform-specific directories

# LOG_FILE=/path/to/custom/nuaa-cli.log

# =============================================================================
# Accessibility Settings (Optional)
# =============================================================================
# Set accessibility output mode
# Options: standard, screen-reader, high-contrast, dyslexia-friendly
# Default: standard

# NUAA_ACCESSIBILITY_MODE=standard

# Enable/disable color output
# Default: auto (detects terminal capabilities)
# Options: auto, always, never

# NO_COLOR=0  # Set to 1 to disable colors
# FORCE_COLOR=1  # Set to 1 to force colors

# =============================================================================
# Advanced Configuration (Optional)
# =============================================================================

# Custom template repository (for forks or alternative sources)
# Default: zophiezlan/nuaa-cli

# NUAA_REPO_OWNER=zophiezlan
# NUAA_REPO_NAME=nuaa-cli
# NUAA_TEMPLATE_BRANCH=main

# HTTP/Network Configuration
# Request timeout in seconds
# Default: 30

# NUAA_HTTP_TIMEOUT=30

# Proxy settings (if behind corporate proxy)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# =============================================================================
# Development & Debug Settings (Optional)
# =============================================================================
# Enable debug mode (more verbose output)

# DEBUG=0  # Set to 1 to enable

# Skip SSL certificate verification (NOT RECOMMENDED for production)
# Only use for development/testing

# NUAA_SKIP_SSL_VERIFY=0  # Set to 1 to skip (insecure)

# =============================================================================
# Notes
# =============================================================================
# - Lines starting with # are comments
# - Remove the # to activate a setting
# - Restart the CLI after changing environment variables
# - Never commit your .env file with real credentials
# - Keep your GitHub tokens secret and secure
