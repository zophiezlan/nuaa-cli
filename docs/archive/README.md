# Archived Files

This directory contains experimental and outdated code that has been archived for reference.

## Files

- **POC_command_factory.py** - Proof of concept for command factory pattern (superseded by `src/nuaa_cli/command_factory.py`)
- **quick-start.py** - Early version of initialization script (functionality now in `src/nuaa_cli/commands/init.py`)

These files are kept for historical reference but are not part of the active codebase.
